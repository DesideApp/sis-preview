{
  "name": "sis-preview",
  "version": "0.1.1-preview.0",
  "private": true,
  "type": "module",
  "license": "SEE LICENSE",
  "engines": { "node": ">=20" },
  "scripts": {
    "lint": "prettier . --check",
    "lint:fix": "prettier . --write",
    "calc:root": "tsx scripts/calc_root.ts fixtures/sample_identity.json fixtures/sample_identity.root.txt",
    "verify:readonly": "tsx scripts/verify_readonly.ts fixtures/sample_identity.json $(cat fixtures/sample_identity.root.txt | tail -n1)"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "prettier": "^2.8.8",
    "tsx": "^4.20.3",
    "typescript": "^5.9.2"
  }
}
